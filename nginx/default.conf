server {
    listen 80;
    server_name localhost;

    root /var/www/app/public;
    index index.php;

    # Symfony / API routing
    location / {
        try_files $uri /index.php$is_args$args;
    }

    # PHP-FPM handling
    location ~ \.php$ {
        include fastcgi_params;
        # use the service name and port defined in docker-compose for PHP-FPM
        # fastcgi_pass app:9000; # Assuming the PHP-FPM service is named 'app' in docker-compose
        # If you are not using docker-compose, use the following line to connect to PHP-FPM directly:
        fastcgi_pass 127.0.0.1:9000;
        fastcgi_index index.php;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
    }

    # Static assets caching
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg)$ {
        expires 30d;
        add_header Cache-Control "public, immutable";
    }

    # Deny access to sensitive files
    location ~ /\.(env|ht|git|json)$ {
        deny all;
    }

    # Security headers
    add_header X-Frame-Options "SAMEORIGIN";
    add_header X-Content-Type-Options "nosniff";
    add_header X-XSS-Protection "1; mode=block";
}
